plugins {
    id "com.android.application"
}

android {
    namespace = "com.example.hazedetector"
    compileSdk = 35

    defaultConfig {
        applicationId = "com.example.hazedetector"
        minSdk = 23
        targetSdk = 35
        versionCode = 1
        versionName = "1.0.0"
    }

    signingConfigs {
        debugKeystore {
            storeFile = file("debug.keystore")
            storePassword = "android"
            keyAlias = "androiddebugkey"
            keyPassword = "android"
        }
    }

    buildTypes {
        debug {
            signingConfig = signingConfigs.debugKeystore
        }

        release {
            signingConfig = signingConfigs.debugKeystore
        }
    }
}
